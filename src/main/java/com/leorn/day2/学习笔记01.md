- 今天学习了**流程控制**,大抵上分为两个结构:**分支结构**和**循环结构**(还没学)
- 分支结构:

> ###if-else与switch

- 了解了异常,写了一个简单的异常类,调用时返回message文本
- 了解equals方法到还有这种写法:
      "A".equals(变量)-->比较通过变量寻找到的对象内容与A是否相等
- 了解到一些规范写法:
      else可以直接写在"if(){};"的大括号后半括号所在行
      如果还要进行分支判断,在else+空格后再写if
- 犯了一点老问题:
      调用scanner后,不关闭tool变量
      忘记写main函数,直接写代码...
      package总是忘了写,没规范代码归属