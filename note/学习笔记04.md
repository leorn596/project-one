今天的学习,其实量也不多,写了三个类,更新了第三天的包

学了类的继承和接口的实现(真的有吗?好像有?)
extend&implement

super()方法用于继承父类的参数
this方法用于指定成员变量

当然,今天的get&set,构造方法也都是用快捷键写的

重写了继承于Student类的introduce()方法,新增了Major参数,直接用+进行字符串拼接,朴实无华

更新了Student类,现在Student类实现了Comparable接口,重写了其中的compareTo()方法,它可以用来比较对象的参数,当然,得手动调整

写了SchoolTest类,这个类里定义了ArrayList<Student>,把Student对象的路径全写进去了,它可以调用size()方法,得知Student对象的个数,可以用for循环遍历列表写入临时的路径,创建临时对象,然后用compareTo()方法比较各个临时对象的值与列表中对象的值,得出列表中含有某个参数最大值的对象
也可以用for循环遍历+introduce方法输出所有的已存对象的自我介绍

写了一个Main类,但好像没怎么用到

对了ArrayList列表对象可以调用其中的remove()方法,移除内部的已存放的对象参数,可以用get()方法获取列表对象中存储的对象路径,还是很好玩的